apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: fundabiz
  labels:
    app: notification
spec:
  type: ClusterIP
  selector:
    app: notification
  ports:
    - port: 4008
      targetPort: 4008
      protocol: TCP
      name: http
