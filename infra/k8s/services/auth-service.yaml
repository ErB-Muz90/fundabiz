apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: fundabiz
  labels:
    app: auth
spec:
  type: ClusterIP
  selector:
    app: auth
  ports:
    - port: 4001
      targetPort: 4001
      protocol: TCP
      name: http
