apiVersion: v1
kind: Service
metadata:
  name: loan
  namespace: fundabiz
  labels:
    app: loan
spec:
  type: ClusterIP
  selector:
    app: loan
  ports:
    - port: 4002
      targetPort: 4002
      protocol: TCP
      name: http
