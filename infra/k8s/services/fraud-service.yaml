apiVersion: v1
kind: Service
metadata:
  name: fraud
  namespace: fundabiz
  labels:
    app: fraud
spec:
  type: ClusterIP
  selector:
    app: fraud
  ports:
    - port: 4007
      targetPort: 4007
      protocol: TCP
      name: http
